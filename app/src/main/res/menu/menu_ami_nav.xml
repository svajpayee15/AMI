<?xml version="1.0" encoding="utf-8"?>
<!--
  The four top-level destinations. Four rather than five so each label can stay at
  15sp without ellipsising on a 360dp screen; the wellbeing story sits one level
  under Health, which is where it belongs anyway.

  Order is the order of the day: what is happening now, the body, the record, setup.
-->
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/nav_home"
        android:icon="@drawable/ic_nav_home"
        android:title="@string/nav_home" />

    <item
        android:id="@+id/nav_health"
        android:icon="@drawable/ic_nav_health"
        android:title="@string/nav_health" />

    <item
        android:id="@+id/nav_report"
        android:icon="@drawable/ic_nav_report"
        android:title="@string/nav_report" />

    <item
        android:id="@+id/nav_settings"
        android:icon="@drawable/ic_nav_settings"
        android:title="@string/nav_settings" />

</menu>
