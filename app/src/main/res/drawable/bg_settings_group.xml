<?xml version="1.0" encoding="utf-8"?>
<!-- Grouped-list container. Same corner radius as AmiCard so the two read as one family. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/ami_surface" />
    <corners android:radius="@dimen/card_radius" />
    <stroke android:width="1dp" android:color="@color/ami_border" />
</shape>
