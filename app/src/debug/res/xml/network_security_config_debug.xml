<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only cleartext exception for testing the escalation backend on a local
  network (e.g. the Android emulator's host loopback at 10.0.2.2, or a LAN dev
  machine). This file lives under src/debug so it is never merged into a
  release build - release traffic stays HTTPS-only by the platform default.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
